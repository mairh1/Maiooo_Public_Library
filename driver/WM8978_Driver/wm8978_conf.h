/**
 * @file    wm8978_conf.h
 * @brief   WM8978 驱动编译期配置入口（当前版本无功能裁剪项）
 * @details 五件套固定文件之一。当前版本行为完全固定，没有可选功能
 *          开关，也没有可调的编译期默认值，因此本文件不定义任何
 *          配置宏。保留本文件作为未来功能裁剪与配置的唯一入口：
 *          后续新增配置时集中在此，按 #ifndef 默认值写法书写，可
 *          用编译器 -D 覆盖，不需要改动驱动核心。
 * @note    当前版本工程无需包含本文件；包含它也不会产生任何宏或
 *          符号。现有可变量均为运行期参数：控制事务超时由
 *          wm8978_bind() 传入，VMID 稳定延时随板级电容不同、由
 *          上电 API 调用者传入，均不属于编译期配置。
 * @author  Maiooo
 * @version 1.0.0
 * @date    2026-08-13
 *
 * SPDX-License-Identifier: WTFPL
 */

#ifndef WM8978_CONF_H
#define WM8978_CONF_H

#ifdef __cplusplus
extern "C" {
#endif

/*
 * 当前版本没有可选功能与编译期默认值，原因：
 * - 核心为纯 C99，无动态内存、无平台依赖，无可裁剪的子系统；
 * - 影子寄存器与保留位保护是写式控制接口的正确性前提，不作为
 *   可关闭的“功能”；
 * - 超时与 VMID 稳定时间依赖板级实测，见文件头 @note。
 * 在没有真实用途前，不编造任何配置开关。
 */

#ifdef __cplusplus
}
#endif

#endif /* WM8978_CONF_H */
